# Anime Telegram Bot

## 📥 Installation & Setup
1.  Clone project:
    ```bash
    git clone https://github.com/nimazerobit/zbanimebot.git
    cd zbanimebot
    ```
2.  Install requirements:
    ```bash
    pip install -r requirements.txt
    ```
3.  Modify config files:
    -   RENAME `config-example.json` to `config.json` and modify it
4.  Run the bot:
    ```bash
    python main.py
    ```

## 👍 Contributing
Open issues for improvements.